# Python
__pycache__/
*.pyc

# Regenerable data caches (parquet built by pipeline/) — ~150 MB, one file >80 MB
# exceeds GitHub limits. Rebuild locally: python3 pipeline/build_events*.py then
# pipeline/backtest_prern*.py
cache/

# Build / run logs
*.log

# Personal LIVE trading signals (runtime state, not for a public repo)
out/live_signals.csv

# Personal executed-trade ledger (real entries/exits and P&L, not for a public repo)
straddles.csv
